# HybridRE
HybridRE is a confidence-guided relation extraction framework that combines pretrained language models (PLMs) and LoRA-adapted large language models (LLMs). The system leverages PLM confidence scores to identify uncertain predictions and selectively routes them to an LLM for reclassification.
